# composites

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build composites` to build the library.

## Running unit tests

Run `nx test composites` to execute the unit tests via [Jest](https://jestjs.io).
