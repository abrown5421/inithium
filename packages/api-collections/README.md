# api-collections

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build api-collections` to build the library.

## Running unit tests

Run `nx test api-collections` to execute the unit tests via [Jest](https://jestjs.io).
