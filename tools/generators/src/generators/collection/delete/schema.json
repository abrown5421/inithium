{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "id": "delete-collection",
  "title": "Delete API Collection",
  "description": "Remove an existing API collection and all associated scaffolding",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the collection to delete in kebab-case (e.g. blog-posts)",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Which collection do you want to delete? (kebab-case)"
    }
  },
  "required": ["name"]
}
